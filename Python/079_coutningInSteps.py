# DAILY CODE MODE
# def step_count(a, b, steps):
#     distance_between_steps = b-a
#     distance_per_step = distance_between_steps/(steps -1 )
#     return [(a + x * distance_per_step) for x in range(steps ) ]
#
# print(step_count(18.75, -22.00, 5))
#
# Answer by akadeco
# def step_count(a, b, steps):
#     step = (b-a) / (steps-1)
#     return [a + step*i for i in xrange(steps)]
#
# SUMMARY
# All answers for this one identical so nothing really to compare to.